# Hands-on labs

Labs turn architecture guidance into safe, reproducible practice.

Every lab must define prerequisites, estimated time, cost expectations, cleanup,
validation, rollback, and fictional identifiers. Production changes are never
automatic.

## Initial lab backlog

- Build a sandbox multi-account structure
- Deny unapproved AWS Regions with SCPs
- Detect and investigate landing zone drift
- Protect Terraform state and delivery pipelines
- Enforce Kubernetes workload security controls
